# Implementation summary

Reverted the local HTML-to-text content parser and changed Aigupiao news parsing to use the API's `web_content` value directly as canonical content. Leading bracketed title extraction now operates on `web_content`, and the resulting body is retained as both the model content and `raw_json.content`. Replaced the HTML-cleaner unit test with a real-response regression check that canonical content contains no HTML markup.

# Copy of the current TODO list

- [x] Read canonical Aigupiao content directly from `web_content`.
- [x] Remove the Aigupiao content HTML-cleaning implementation and its focused tests.
- [x] Verify stored Aigupiao content from real fixtures contains no HTML markup.
- [x] Run the Aigupiao collector test suite and record the implementation pass history.

# Related commit list

- `d9b5b9d feat(collector): bootstrap news collection service`

# Requirement content

Revert the earlier code that filters HTML information from `content`. Read canonical `content` directly from `raw_json.web_content`. The content regression test only needs to verify that the resulting content contains no HTML information.
