# Implementation summary

Extended news storage with structured theme, topic, stock-market, engagement, and 24-hour-hot metadata. Added a one-calendar-month final-refresh workflow that refreshes the accessible feed through the due boundary and freezes eligible rows for long-term storage. Added metadata and lifecycle tests, documented operation, and consolidated every new database field into the original `CREATE TABLE` definitions because the schema has not yet been deployed.

# Copy of the current TODO list

- [x] Extend metadata parsing for the News Collector service.
- [x] Extend PostgreSQL metadata and engagement storage for the News Collector service.
- [x] Normalize topic relationships for the News Collector service.
- [x] Implement monthly final-refresh lifecycle processing for the News Collector service.
- [x] Add final-refresh and metadata tests for the News Collector service.
- [x] Document final-refresh operation for the News Collector service.
- [x] Verify linting, tests, and packaging for the News Collector service.

# Related commit list

- `d9b5b9d feat(collector): bootstrap news collection service`

# Requirement content

Persist `theme`, `theme_quotes`, `concept_list`, `topic`, `stock_info`, `stock_quotes`, `stock_code`, and `quote_plate`, plus view, like/support, oppose, share, and comment counts. Track collection and update times. Because the upstream query window is approximately two months, perform one final refresh after one month and then transition the entry to immutable long-term storage. Research whether the payload contains a news-pinning indicator. Since the database schema has not yet run, integrate all new columns into the original table definitions and do not retain `ALTER TABLE` migration statements.
