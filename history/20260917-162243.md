# Implementation summary

Implemented the first modular Aigupiao News Collector release. It includes environment-backed configuration, a retrying HTTP client, real-payload parsing with privacy filtering, domain models, transactional PostgreSQL upserts and checkpointing, independent backfill/live workflows, probe and schema-init commands, packaging, operational documentation, and mocked unit tests. The parser was validated against `news.json` and `news2.json`.

# Copy of the current TODO list

- [x] Add packaging and environment configuration for the News Collector service.
- [x] Implement response parsing and privacy sanitization for the News Collector service.
- [x] Implement conservative HTTP retry handling for the News Collector service.
- [x] Implement transactional PostgreSQL persistence and schema setup for the News Collector service.
- [x] Implement backfill checkpoint pagination for the News Collector service.
- [x] Implement live polling for the News Collector service.
- [x] Implement probe and collector command-line entry points for the News Collector service.
- [x] Add unit tests for the News Collector service.
- [x] Document setup and operation for the News Collector service.
- [x] Verify linting and tests for the News Collector service.

# Related commit list

No commits were created for this implementation pass.

# Requirement content

Implement the Python service specified by `REQUIREMENTS.md`: conservatively collect Aigupiao API pages, flatten all date groups, use `rec_time` for historical pagination, stop on a non-advancing cursor, and independently support resumable backfill and live polling. Store idempotent news, stock, and theme records in PostgreSQL while retaining sanitized raw JSON and committing each backfill batch with its checkpoint. Handle timeout, network, 429, 403, 5xx, malformed JSON, and unsuccessful API responses without anti-bot bypasses. Configure the service through environment variables, provide backfill/live/probe CLI commands, keep HTTP/parsing/persistence/orchestration modular, and test parsing, duplicates, cursors, retries, rollback, and restart behavior without production API calls. Use the supplied `news.json` and `news2.json` payloads as response-shape references.
