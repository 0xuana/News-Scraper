# Implementation summary

Changed raw news sanitization to preserve the API's original non-empty `content`, `web_content`, and `content_pc` values. Canonical model content continues to come from `web_content` with its leading bracketed title removed, but that derived body no longer replaces `raw_json.content`. Added real-fixture and focused regression assertions for original content retention and empty-value removal.

# Copy of the current TODO list

- [x] Preserve all non-empty Aigupiao content variants in `raw_json`.
- [x] Add parser regression coverage for original content retention and empty-value removal.
- [x] Run the Aigupiao collector test suite and record the implementation pass history.

# Related commit list

- `d9b5b9d feat(collector): bootstrap news collection service`

# Requirement content

Preserve all original content values in `raw_json`, including `content`, `web_content`, and `content_pc`. Remove them only when their value is empty.
